export default function Loading() {
	return (
		<main className="min-h-[calc(100vh-4rem)] flex items-center justify-center px-6">
			<p className="text-sm text-[var(--muted)] animate-pulse text-center">
				Just a moment — gathering thoughts
			</p>
		</main>
	)
}
